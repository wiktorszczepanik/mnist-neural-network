## Artificial neural network

A simple neural network implementation written from scratch in Go, trained to recognize handwritten digits from the MNIST dataset.

The project implements the main components of a feed-forward neural network and the backpropagation algorithm without relying on external machine learning libraries.